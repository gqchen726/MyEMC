package com.briup.accept;

/**
 * 接收数据
 * 
 * @author ghost
 *
 */
public interface IAccept {
	/**
	 * 接收客户端发送过来的数据 
	 * 1.创建服务端连接 
	 * 2.连接客户端，把连接交给一个新创建的线程去处理 
	 * 3.等待新的客户端的连接
	 */
	void accept();
}
